<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="2dp" />
            <gradient
                android:startColor="@color/header_title_text"
                android:centerColor="@color/header_title_text"
                android:centerY="0.75"
                android:endColor="@color/header_title_text"
                android:angle="270"
                />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="2dp" />
                <gradient
                    android:startColor="@color/header_title_text"
                    android:centerColor="@color/header_title_text"
                    android:centerY="0.75"
                    android:endColor="@color/header_title_text"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
    <item
        android:id="@android:id/progress"
        >
        <clip>
            <shape>
                <corners
                    android:radius="2dp" />
                <gradient
                    android:startColor="@color/header_title_text"
                    android:endColor="@color/header_title_text"
                    android:angle="270" />
            </shape>
        </clip>
    </item>

</layer-list>